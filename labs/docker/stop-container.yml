---
- name: Stop a bunch of containers
  hosts: localhost
  tasks:
  - community.docker.docker_container:
      name: "ansible-{{ item }}"
      image: python
      tty: true
      interactive: true
      state: absent
      force_kill: true
    loop: "{{ range(1,6) }}"
